VITE_SERVER_URL = http://localhost:3000
VITE_ENVIRONMENT = development # Change to "production" for deployment
VITE_RIDE_TIMEOUT = 90000 # ride will automatically cancel after 1.5 minutes if no-one accepts the ride

# Google Maps API Key (necesaria para el frontend)
VITE_GOOGLE_MAPS_API = <your-google-maps-api-key>

# Coordenadas por defecto (San Antonio del Táchira, Venezuela)
VITE_DEFAULT_LAT = 7.8144
VITE_DEFAULT_LNG = -72.4431

# Configuración de notificaciones
VITE_ENABLE_SOUND_NOTIFICATIONS = true

# Radio de llegada del conductor (en metros)
VITE_ARRIVAL_RADIUS = 50

# Intervalo de actualización de ubicación (en milisegundos)
VITE_LOCATION_UPDATE_INTERVAL = 5000